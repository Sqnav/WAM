from __future__ import annotations


def main() -> None:
    raise RuntimeError(
        "Offline target locator evaluation was removed. "
        "The active model now uses an optional body-frame target-relative context token."
    )


if __name__ == "__main__":
    main()
